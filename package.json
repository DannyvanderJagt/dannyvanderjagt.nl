{
  "name": "dannyvanderjagt.nl",
  "version": "1.0.0",
  "description": "",
  "main": "server/dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf server/dist && babel server/src --out-dir server/dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4",
    "serve-favicon": "^2.3.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0"
  }
}
